# 只用于把 Anchor.sol 编译成 contracts/Anchor.json（abi + bytecode）。
# 运行期（部署/锚定/读回）不需要 solc/forge —— 只用到已入库的 Anchor.json 与 `cast`。
[profile.default]
src = "."
out = "out"
libs = []
optimizer = true
optimizer_runs = 200
solc_version = "0.8.24"
evm_version = "cancun"
