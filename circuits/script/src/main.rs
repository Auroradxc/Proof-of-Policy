//! SP1 driver skeleton: generate + verify Proof-of-Policy proofs.
//!
//! W4 fills this in: build the program ELF, load a ProofRequest, prove, verify
//! on the host, and optionally verify on-chain via sp1-contracts.
//!
//! NOTE: not buildable yet — Rust/SP1 toolchain not installed on this machine.

fn main() {
    eprintln!("PoP proving driver — populated in W4 of the 8-week plan.");
    eprintln!("Install Rust + SP1 first (see circuits/README.md).");
}
