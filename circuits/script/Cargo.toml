[package]
name = "pop-script"
version = "0.1.0"
edition = "2021"
description = "SP1 driver: generate and verify Proof-of-Policy proofs"

# Skeleton — not buildable until W4. Pin sp1-sdk to the version matching your
# installed SP1 CLI.
[dependencies]
sp1-sdk = { version = "3" }
# serde, bincode, clap are added in W4.

[workspace]
