[package]
name = "pop-verify"
version = "0.1.0"
edition = "2021"
description = "Verifier-only binary for Proof-of-Policy: verifies compressed/groth16/plonk proofs via sp1-verifier (no prover)"

[dependencies]
sp1-verifier = "6.7.0"
bincode = "1.3"
serde_json = "1"
sha2 = "0.10"
